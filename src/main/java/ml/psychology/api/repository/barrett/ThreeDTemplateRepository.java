package ml.psychology.api.repository.barrett;

import ml.psychology.api.domain.barrett.template.ThreeDTemplate;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ThreeDTemplateRepository extends JpaRepository<ThreeDTemplate, Long> {
}
